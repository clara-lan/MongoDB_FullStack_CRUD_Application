<!DOCTYPE html>
<html>
  <head>
    <title>Songs List</title>
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css"
      integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk"
      crossorigin="anonymous"
    />

    <link rel="stylesheet" href="/stylesheets/home.css" />
  </head>
  <body>
    <%- include('../partials/navbar') %>
    <div id="add-song-title">
      <h3> <a href='/newsong' >Add Your Own Song</a> </h3>
    </div>
      <!-- list out songs -->
        <section id="songs-list">
            <% mySongs.forEach(song => { %>      
              <div class="card m-1" id="song-<%= song._id %>">
                <!-- <img src="<%= song.img %>" class="card-img-top" alt="<%= song.title %>"> -->
                <div class="card-body">
                  <h5 class="card-title"><%= song.title %></h5>
                  <div><%= song.artist%></div>
                  <div><%= song.album%></div>
                  <div><%= song.published%></div>
                  <button data-id="${song._id}" class="btn btn-danger">Remove</button>
                </div>
              </div>
            <% }) %>
          </section>
    </div>
  </body>
</html>
